{
  "name": "acpfx",
  "version": "0.2.0",
  "private": true,
  "description": "Observable DAG-based voice pipeline for ACP agents",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:native": "cargo build -p acpfx-orchestrator",
    "check": "tsc --noEmit",
    "test": "node --test dist/tests/**/*.test.js",
    "start": "./target/debug/acpfx run",
    "start:headless": "./target/debug/acpfx run",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "keywords": [
    "acp",
    "voice",
    "pipeline",
    "cli"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@acpfx/core": "workspace:*",
    "speaker": "^0.5.5",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "zod": "^4.3.6"
  },
  "packageManager": "pnpm@10.29.3",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "speaker"
    ]
  }
}
