[package]
name = "mic-speaker"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mic-speaker"
path = "src/main.rs"

[dependencies]
sys-voice = { path = "../sys-voice" }
serde_json = "1"
base64 = "0.22"
tokio = { version = "1", features = ["rt", "macros"] }
