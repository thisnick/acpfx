[package]
name = "mic-speaker"
version = "0.5.0"
edition = "2021"

[[bin]]
name = "mic-speaker"
path = "src/main.rs"

[dependencies]
sys-voice = { path = "../sys-voice" }
serde_json = "1"
serde_yaml = "0.9"
base64 = "0.22"
tokio = { version = "1", features = ["rt", "macros", "time"] }
