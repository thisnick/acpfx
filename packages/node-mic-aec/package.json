{
  "name": "@acpfx/mic-aec",
  "version": "0.2.0",
  "bin": {
    "acpfx-mic-aec": "./bin/mic-aec"
  },
  "scripts": {
    "build:native": "cargo build --release && mkdir -p bin && cp target/release/mic-aec bin/"
  }
}
