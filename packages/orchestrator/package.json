{
  "name": "@acpfx/cli",
  "version": "0.4.4",
  "description": "Observable audio pipeline framework — pluggable voice agent pipelines via YAML config",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/thisnick/acpfx",
    "directory": "packages/orchestrator"
  },
  "bin": {
    "acpfx": "./bin/acpfx"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin/",
    "scripts/"
  ],
  "scripts": {
    "build:native": "cargo build --release -p acpfx-orchestrator",
    "postinstall": "node scripts/postinstall.js"
  }
}
